@model IEnumerable<prog6212_part3_ST10456157.Models.Claim>

<h2>Coordinator Dashboard</h2>

<table class="table table-hover">
    <thead>
        <tr>
            <th>ID</th>
            <th>Lecturer</th>
            <th>Hours</th>
            <th>Rate</th>
            <th>Total</th>
            <th>Document</th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var c in Model)
        {
            <tr>
                <td>@c.ClaimId</td>
                <td>@c.LecturerName</td>
                <td>@c.HoursWorked</td>
                <td>@c.HourlyRate</td>
                <td>@(c.HoursWorked* c.HourlyRate)</td>
                <td>
                    @if (!string.IsNullOrEmpty(c.DocumentName))
                    {
                        <a href="~/uploads/@c.DocumentName" target="_blank">View File</a>
                    }
                </td>
                <td>
                    <form asp-action="Verify" method="post" class="d-inline">
                        <input type="hidden" name="id" value="@c.ClaimId" />
                        <button class="btn btn-success btn-sm">Verify</button>
                    </form>
                    <form asp-action="Reject" method="post" class="d-inline">
                        <input type="hidden" name="id" value="@c.ClaimId" />
                        <button class="btn btn-danger btn-sm">Reject</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>

